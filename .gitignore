# dependencies & build output
node_modules/
dist/

# secrets & local env
.env

# python
__pycache__/
*.pyc

# os / editor
.DS_Store
*.tmp
*.log

# runtime artifacts
family_raw.json

# internal-only docs — MUST NOT be published (contain private terms / audit history).
# These stay untracked so they can never leak via git history when the repo goes public.
OPEN-SOURCE-PLAN.md
AUDIT-REGISTER.md
